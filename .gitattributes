# Normalize line endings so a Windows checkout does not produce whole-file
# diffs for collaborators on macOS/Linux or in CI.
* text=auto eol=lf

*.ico binary
*.png binary
*.jpg binary
*.woff2 binary
